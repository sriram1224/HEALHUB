import { useContext } from "react";
import { AppContext } from "../context/AppContext";

const RelatedDoctors = () => {
    const { doctors } = useContext(AppContext);
    const [reldocs,setrek] =  
  return <div></div>;
};

export default RelatedDoctors;
